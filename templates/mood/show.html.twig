{% extends 'base.html.twig' %}

{% block title %}Mood{% endblock %}

{% block body %}
    <h1>Mood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mood.id }}</td>
            </tr>
            <tr>
                <th>Feeling</th>
                <td>{{ mood.feeling }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ mood.date ? mood.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mood_index') }}">back to list</a>

    <a href="{{ path('mood_edit', {'id': mood.id}) }}">edit</a>

    {{ include('mood/_delete_form.html.twig') }}
{% endblock %}
